{
    "name": "STEM-C",
    "stack": "container",
    "addons": [
        {
          "plan": "heroku-postgresql",
          "as": "DATABASE"
        }
    ],
    "scripts": {
      "postdeploy": "./scripts/init_db.sh"
    },
    "env": {
      "APP_ENVIRONMENT": "review",
      "DATABASE_URL": {
        "required": true
      }
    }
}