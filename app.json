{
    "name": "STEM-C",
    "stack": "container",
    "addons": [
        {
          "plan": "heroku-postgresql",
          "as": "DATABASE"
        }
    ],
    "scripts": {
      "postdeploy": "psql $DATABASE_URL < ./scripts/test_db.dump"
    }
  }