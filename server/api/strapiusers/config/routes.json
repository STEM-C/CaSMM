{
    "routes": [
      {
        "method": "GET",
        "path": "/strapiusers/super-admin",
        "handler": "strapiusers.findSuperAdmins",
        "config": {
          "policies": []
        }
      }
    ]
    }