{
    "routes": [
        {
            "method": "GET",
            "path": "/sandbox/toolbox",
            "handler": "sandbox.toolbox",
            "config": {
                "policies": []
            }
        }
    ]
}